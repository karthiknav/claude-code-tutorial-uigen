Look at the most recent Q&A exchange in this conversation and append it to `info.md` at the project root, following the existing format:

- Use `## Q: <question>` as the heading
- Include the full answer below it
- Add a `---` separator at the end

Do not rewrite or duplicate any existing content in `info.md`. Only append the new exchange.
